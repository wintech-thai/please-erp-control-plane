apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: please-erp-tenant-ing
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.org/client-max-body-size: 50m
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.pleaseErpTenantDomain }}
  rules:
    - host: {{ .Values.pleaseErpTenantDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: please-erp-{{ .Values.envGroup }}-tenant
                port:
                  number: 80
